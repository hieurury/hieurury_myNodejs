<div class="row" style="margin: 0;">
    {{#if group.videos}}
        <div class="col-8"  >
        {{#with currentVideo}}
            <div style="text-align: center; background:#000;" class="col-12">
                <iframe width="100%" height="580px" src="{{link}}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>
            </div>
            <h3 class="video-name">{{title}}</h3>
        {{/with}}
            <div class="block-button">
                <div class="button-control">
                    <a href="/" class="button-normal home-page--btn">Trang chủ</a>
                </div>
            </div>
        </div>

        <div class="col-4">
        {{#with group}}
            <h3>Video hiện tại</h3>
            <div class="list-group">
            {{#each videos}}
                <a href="/groups/watch/{{../slug}}?idVideo={{this._id}}" class="list-group-item list-group-item-action">{{this.title}}</a>
            {{/each}}
            </div>
        {{/with}}
        </div>
    {{else}}
    <div class="col-12">
        <div class="block-notifile">
            <h1 class="">GROUP NÀY CHƯA CÓ VIDEO NÀO!</h1>
            <img src="/img/plane_2.gif" alt="" class="notifile-img">
            <a href="/" class="button-normal">Về trang chủ thoai!</a>
        </div>
    </div>
    {{/if}}
</div>

<script>
    const videoTitle = document.querySelector(".video-name");
    const chooseTitle = document.querySelectorAll(".list-group a");

    console.log(videoTitle.textContent)
    for(let title of chooseTitle) {
        let content = title.textContent;
    console.log(content)
        if(title.classList.contains("active")) {
            title.classList.remove("active");
            console.log(true)
        }
        if(content == videoTitle.textContent) {
            title.classList.add("active");
        }
    }
</script>